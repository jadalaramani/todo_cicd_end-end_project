replicaCount: 1

image:
  repository: ramanijadala/todo-app
  tag: build-1      # Replace with correct tag or use --set
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

resources:
  limits:
    cpu: 500m
    memory: 256Mi
  requests:
    cpu: 250m
    memory: 128Mi
